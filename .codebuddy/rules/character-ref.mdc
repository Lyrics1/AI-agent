---
description: 角色参考图（设定图）抽取与生成的强制规则。当进行"抽角色""角色参考图""角色设定图""角色图""从小说提取角色"，或处于小说→分镜→视频管线的"角色资产准备"阶段时适用。完整可执行流程见 .codebuddy/skills/character-ref/SKILL.md。
globs:
alwaysApply: false
---

# 角色参考图抽取规则

> 完整可执行流程（含 SOP、示例）见 `.codebuddy/skills/character-ref/SKILL.md`。本文件为常驻速查规则。

## 1. 角色判别方法
同时满足：会自主行动、有意志、有视觉形象、是动作/声音的发出者。

## 2. 提取规则
出场次数 ≥ 2。计数单位：1 次出场 = 1 个分镜中该角色有视觉呈现（不是"被提及次数"）。

## 3. 排除规则
仅在引文/证词/新闻字幕/旁白/画外音中被提及，但全程无视觉呈现的角色，不提取。

## 4. 视觉呈现的广义认定（强制）
以下情形均视为"有视觉呈现"，计入出场次数：
1. 角色在画面中直接出场（现场/实地）；
2. 角色出现在画面中展示的照片/画像/影像/通缉令/海报/屏幕画面/信件附图中（只要该载体在分镜中被特写或展示）；
3. 角色在回忆/闪回画面中出现。

## 5. 剧情关键角色强制提取（即使出场仅 1 次）
仅视觉呈现 1 次（含照片/画像），但满足任一条件即必须提取：
- 被 ≥ 3 个不同角色在对话中提及；或
- 是主线剧情核心线索人物（主角直属上司、幕后黑手、关键证人、被害者等），推进主线 ≥ 1 次。

## 6. 子类型
人物 / 动物 / 怪物 / 丧尸 / AI / 幽灵 / 活物 等，按角色本质判定。

## 7. 去重与合并
- 同一角色多称呼 → 合并为 1 个，取正式名；
- 同一角色不同年龄段 → 剧情需对比则拆 2 个（加年龄后缀），否则合并；
- 群像角色（如"三个士兵"）→ 无个体差异抽 1 个代表并注明"群像"，有差异则按个体分别抽取。

## 8. 命名规则
`角色N_{子类型}_{角色名}`
- N 从 1 递增，按首次出场顺序；
- 角色名：有名字用名字；无名字用 2-8 字描述（如 `枪手`、`战马赤兔`、`第1位观众`）；
- 文件名禁用 `/ \ : * ? " < > |` 及全角引号，遇到则替换为下划线或省略。

## 9. 输出规范
- 保存位置：`{小说目录}/参考图提示词/{角色命名规则}.md`
- 画面风格：执行前与用户统一确认，全角色共用同一风格（禁止硬编码默认值）。
- 提示词模板见 SKILL.md。

## 10. 强制约束（不可违反）
1. 所有角色共用同一画面风格。
2. 提示词中**不得**出现声音属性（音色/语速/语气）——图像模型无法理解且会污染画面；声音信息另存 `角色档案.md`。
3. 原文摘抄严格限 1-2 句，只描写目标角色本身，不夹带剧情与其他角色。
4. 落盘前与用户对齐最终角色清单，避免返工。
5. 提示词文件产出后，用 `lingya generate image` 生成设定图；图片作为后续 `lingya generate video --subject-ref` 的角色参考主体。

## 11. 文档保存格式
- 规则/说明类文档统一保存为 `.md`，不使用 `.docx`。
