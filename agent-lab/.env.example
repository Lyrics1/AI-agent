# 复制此文件为 .env，按你选用的平台填一组配置（三个平台任选其一，OpenAI 兼容）

# ---- 方案 1：DeepSeek（需充值，便宜，能力强）----
LLM_API_KEY=sk-你的真实key
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat

# ---- 方案 2：智谱 GLM（有免费模型，推荐零成本起步）----
# 注册 https://bigmodel.cn 拿 key，把上面三行注释掉，启用下面三行：
# LLM_API_KEY=你的智谱key
# LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# LLM_MODEL=glm-4-flash-250414

# ---- 方案 3：本地 Ollama（完全免费，无需 key）----
# 先 `ollama run qwen2.5:7b`，然后启用下面三行：
# LLM_API_KEY=ollama
# LLM_BASE_URL=http://localhost:11434/v1
# LLM_MODEL=qwen2.5:7b
